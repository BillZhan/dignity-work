# 2026-09-08 工作日志

## Dignity·Work 公益网站 MVP 开发

从零开发了「让每一份劳动，都有尊严」公益 Web MVP，完整可运行并已静态导出。

### 技术栈
- Next.js 14 (App Router) + TypeScript + Tailwind CSS + lucide-react
- Supabase 双模式：配置 env 走真实后端，未配置走本地 mock（localStorage 持久化）
- 静态导出（output: export）适配 Cloudflare Pages / GitHub Pages 免费部署

### 第一阶段：核心开发（上午）
- 6 大页面：首页 / 企业列表 / 企业详情 / 推荐+纠错 / 关于我们 / 后台管理
- 8 家示例企业 mock 数据（含 6 项权益标签 + 验证等级 L0-L4）
- 完整数据库 Schema：companies / labor_rights / submissions / supports / reports / admin_users + RLS + 触发器
- 后台管理：密码登录（默认 dignity2026）+ 仪表板/企业管理/提交审核/举报处理
- 支持去重（visitor_id localStorage）
- GitHub Actions 部署脚本 + README + 免责声明

### 关键实现细节
- 动态路由 [slug] 拆为 server(page.tsx + generateStaticParams) + client(CompanyDetailClient) 以支持静态导出预渲染
- PostCSS 配置需用 .cjs + CommonJS（否则 Next.js 报 "must export plugins key"）
- 支持数在 mock 模式用 localStorage 记录增量，避免重复刷票

### Bug 修复：卡片标签竖排挤压（中午）
- 根因：RightBadge sm 尺寸写死 w-7 h-7（28px 宽），中文标签放不下逐字换行成竖排
- 修复：去掉固定 w/h，inline-flex + whitespace-nowrap + 自适应宽度
- 增加 short 标签（社保/双休/加班费/合同/福利/安全）用于卡片，详情页保留完整标签

### 第二阶段：上线准备（中午）
- 扩充真实企业数据：8 → 26 家，覆盖餐饮/科技/零售/制造/物流/金融/教育/医疗/服务
- 每家标注 L0-L2 验证等级 + 公开资料来源
- 数据源抽离到 data/companies.json（单一来源），lib/mock-data.ts 与 scripts/generate-seed.mjs 共用
- 生成 supabase/seed.sql（26 家企业 INSERT 语句）供 Supabase 一键导入

### 三份完整文档
- README.md — 项目说明 + 文档索引
- QUICKSTART.md — 5 分钟快速开始
- DEPLOY.md — Cloudflare Pages / GitHub Pages 详细部署
- SUPABASE_SETUP.md — Supabase 接入指南（含 RLS 解释、限额、故障排查）

### 编译结果（最终）
- 34 个静态页面全部生成（首页 + 列表 + 26 详情 + 提交 + 关于 + 后台 + 404）
- 零错误零警告
- 预览服务器：python http.server 4173，home:200 / detail:200

### 待办（V2）
- 真实账户体系、企业认证、评论、微信小程序
- Cloudflare Images 替换 Unsplash、SEO sitemap 自动生成

### 第三阶段：GitHub 推送 ✅（下午 13:39-13:48）
- 用户 BillZhan 提供 PAT（repo scope），要求我帮他部署
- 用户偏好进一步明确：**希望 AI 直接动手**，而不是给指引让他自己跑
- 第一次推送失败：mihomo 代理端口问题（环境变量 HTTPS_PROXY=127.0.0.1:51466，但 mihomo 实际监听 7890）
- 解决方案：`git -c http.proxy=http://127.0.0.1:7890 push ...`
- 第二次推送失败：GitHub 拒绝 PAT 推送 `.github/workflows/deploy.yml`（缺 workflow scope）
- 解决方案：git rm --cached deploy.yml → git commit --amend → push 成功
- **仓库 52 个路径全部推送成功**（HEAD = 28d07de）
- 仓库地址：https://github.com/BillZhan/dignity-work
- deploy.yml 保留在本地未跟踪，等用户重新生成带 workflow scope 的 Token 后再补推
- DEPLOY.md 已更新两个避坑要点

### 沉淀经验（写进 DEPLOY.md）
- GitHub PAT 必须勾 `workflow` 才能推 Actions 文件
- Windows + mihomo + git 必须显式配代理（环境变量 HTTPS_PROXY 每次启动端口不同，git 不会自动用 mihomo）